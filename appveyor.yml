# Version format
version: 1.0{build}

# Build worker image
image: Visual Studio 2017

# Build platform
platform: Any CPU

# Build Configuration
configuration: Debug

# MSBuild settings
build:
  project: src\Route4Me Route Optimization Examples.sln
  verbosity: quiet

# Execute script before build
before_build:
 - nuget src\restore Route4Me Route Optimization Examples.sln
 
 only_commits:
  files:
    - src\Route4MeSDKLibrary\

# Omitted the tests, as they are not relevant for the question