# Version format
version: 1.0{build}

# Build worker image
image: Visual Studio 2017

# Build platform
platform: Any CPU

# Build Configuration
configuration: Debug

# MSBuild settings
build:
  - project: route4me-vbnet-sdk\Route4MeSDKLibrary.vbproj

# Execute script before build
before_build:
 - nuget restore route4me-vbnet-sdk\Route4Me Route Optimization Examples.sln
 
 only_commits:
  files:
    - route4me-vbnet-sdk\Route4MeSDKLibrary\

# Omitted the tests, as they are not relevant for the question