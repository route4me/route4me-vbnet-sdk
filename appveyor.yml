# Version format
version: 2.0.1.0{build}

# Build worker image
image: Visual Studio 2017

# Build platform
platform: Any CPU

# Build Configuration
configuration: Debug

# MSBuild settings
build:
  project: Route4Me Route Optimization Examples.sln
  verbosity: quiet

# Execute script before build
before_build:
 - nuget restore Route4Me Route Optimization Examples.sln

# Omitted the tests, as they are not relevant for the question